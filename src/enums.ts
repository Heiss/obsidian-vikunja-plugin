enum supportedTasksPluginsFormat {
	Emoji
}

enum chooseOutputFile {
	DailyNote,
	File
}

enum backendToFindTasks {
	Dataview
}

export {supportedTasksPluginsFormat, chooseOutputFile, backendToFindTasks};
